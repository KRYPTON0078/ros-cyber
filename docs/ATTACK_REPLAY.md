# Attack Replay Timeline

The SOC dashboard renders a replay timeline from recent alerts.

## Data Source

`GET /api/v1/alerts` returns the latest alerts with timestamps.

## Display

The UI sorts alerts by most recent and shows severity, title, and time.
