# Report Exports

Dashboard exports are available in CSV and JSON:

- Alerts: `/api/v1/report/alerts.csv` and `/api/v1/report/alerts.json`
- Audit: `/api/v1/report/audit.csv` and `/api/v1/report/audit.json`

Use the dashboard action buttons or the PowerShell helpers in `scripts/`.
